# env.example - Documentation of required Doppler secrets
# These are NOT loaded from a file. They live in Doppler (project: mani).
# Run: doppler setup --config dev_personal

# ─── PostgreSQL / Supabase ────────────────────────────────────────────────────
POSTGRES_PASSWORD=your-super-secret-password
JWT_SECRET=your-jwt-secret-at-least-32-chars-long
SUPABASE_ANON_KEY=             # JWT with role=anon, generated from JWT_SECRET
SUPABASE_SERVICE_ROLE_KEY=     # JWT with role=service_role, generated from JWT_SECRET
DASHBOARD_USERNAME=supabase
DASHBOARD_PASSWORD=your-dashboard-password

# ─── Manifold Self-Hosting ────────────────────────────────────────────────────
SELF_HOSTED=true
SUPABASE_DB_HOST=localhost     # local dev: localhost; prod (Docker): db
SUPABASE_DB_PORT=5432
SUPABASE_PASSWORD=your-super-secret-password   # same as POSTGRES_PASSWORD
SUPABASE_KEY=                  # same as SUPABASE_SERVICE_ROLE_KEY
SUPABASE_URL=http://localhost:8000  # local dev: localhost:8000; prod (Docker): http://kong:8000
SUPABASE_JWT_SECRET=           # same as JWT_SECRET

# ─── Manifold API ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=localhost:8088          # no http:// prefix, code adds it
NEXT_PUBLIC_FIREBASE_ENV=DEV

# ─── Manifold Web ─────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=http://localhost:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY= # same as SUPABASE_ANON_KEY

# ─── Firebase / GCP Auth ─────────────────────────────────────────────────────
# Uses Application Default Credentials (no service account key needed).
# Run: gcloud auth application-default login
# The ADC file is mounted into containers automatically.
GOOGLE_CLOUD_PROJECT=manifold-fd657   # your Firebase project ID
# GOOGLE_ADC_PATH=~/.config/gcloud/application_default_credentials.json  # default, override if needed

# ─── Production Domain ────────────────────────────────────────────────────────
SITE_URL=https://manifold.mikhailtal.dev

# ─── Optional (skip initially) ───────────────────────────────────────────────
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# STRIPE_APIKEY=
# STRIPE_WEBHOOKSECRET=
# MAILGUN_KEY=
# TWILIO_AUTH_TOKEN=
# TWILIO_SID=
